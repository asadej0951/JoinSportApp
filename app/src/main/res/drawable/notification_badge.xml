<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <solid android:color="@color/colorBadge" />

    <stroke
        android:width="2dp"
        android:color="@color/colorBadge" />

    <corners
        android:bottomLeftRadius="@dimen/d10dp"
        android:bottomRightRadius="@dimen/d10dp"
        android:topLeftRadius="@dimen/d10dp"
        android:topRightRadius="@dimen/d10dp" />

</shape>