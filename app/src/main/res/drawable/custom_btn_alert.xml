<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- disable state. -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/d12dp"/>
            <solid android:color="#AAAAAA"/>
            <stroke android:color="#AAAAAA" android:width="2dp"/>
        </shape>
    </item>

    <!--released state. -->
    <item android:state_pressed="false">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/d12dp"/>
            <solid android:color="@color/color_main"/>
            <stroke android:color="@color/color_main" android:width="2dp"/>
        </shape>
    </item>

    <!--pressed state. -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/d12dp"/>
            <solid android:color="@color/color_main_click"/>
            <stroke android:color="@color/color_main_click" android:width="2dp"/>
        </shape>
    </item>

</selector>
